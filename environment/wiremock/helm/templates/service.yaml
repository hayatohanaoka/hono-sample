apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}-svc
spec:
  selector:
    name: {{ .Values.name }}
  ports:
    - name: {{ .Values.name }}-svc-port
      port: {{ .Values.port }}
      targetPort: http-port
