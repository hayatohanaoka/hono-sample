apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.name }}-svc"
  labels:
    app: "{{ .Values.name }}-svc"
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.name }}
  ports:
    - name: "{{ .Values.name }}-svc-port"
      port: 13000
      targetPort: http-port
